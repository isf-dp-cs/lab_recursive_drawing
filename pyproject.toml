[tool.poetry]
name = "lab_recursive_drawing"
version = "0.1.0"
description = ""
authors = ["britte <bgenzlinger@isf.edu.hk>"]
readme = "README.md"
package-mode = false

[tool.poetry.dependencies]
python = "^3.10"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"